// import PureCounter from "@srexi/purecounterjs";

export default defineNuxtPlugin((nuxtApp) => {

    if (typeof window !== "undefined") {
        // const pure = new PureCounter();
        // nuxtApp = pure
        // mounted (el) {
            // nuxtApp.use(PureCounter)
        // return new PureCounter();
        return ;
    }

})